{
  "name": "obisync-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.ts",
    "dev": "node --watch server.ts",
    "db:setup": "node scripts/setupDatabase.ts",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "node --test auth/*.test.ts Classes/*.test.ts users/*.test.ts serverConfig.test.ts"
  }
}
